{
	"name": "@essex/build",
	"private": true,
	"license": "MIT",
	"contributors": [
		"David Tittsworth <datit@microsoft.com>",
		"Chris Trevino <chtrevin@microsoft.com>"
	],
	"packageManager": "yarn@3.2.0",
	"scripts": {
		"preinstall": "npx only-allow yarn",
		"postinstall": "husky install",
		"clean_primary": "yarn workspaces foreach --exclude '@essex/build' --exclude '@essex/test-*' -piv run clean",
		"clean_tests": "yarn workspaces foreach --include '@essex/test-*' -piv run clean",
		"clean": "run-s clean_tests clean_primary",
		"build": "yarn workspaces foreach -piv --topological-dev run build",
		"lint": "yarn workspaces foreach -piv --all run lint",
		"test": "yarn workspaces foreach -piv --all run test",
		"version": "yarn workspaces foreach --all -ivd version",
		"release": "yarn workspaces foreach --all -pv npm publish --tolerate-republish --access public",
		"jest": "jest",
		"eslint": "ESSEX_DEBUG=1 yarn essex lint --fix",
		"prettify": "essex prettify",
		"git_is_clean": "yarn essex git-is-clean",
		"ci": "run-s build lint eslint test jest git_is_clean"
	},
	"devDependencies": {
		"@essex/babel-config": "workspace:packages/config-babel",
		"@essex/eslint-config": "workspace:packages/config-eslint",
		"@essex/eslint-plugin": "workspace:packages/config-eslint-plugin",
		"@essex/jest-config": "workspace:packages/config-jest",
		"@essex/prettier-config": "workspace:packages/config-prettier",
		"@essex/scripts": "workspace:packages/scripts",
		"@types/eslint": "^8.4.1",
		"@types/jest": "^27.4.1",
		"@typescript-eslint/eslint-plugin": "^5.13.0",
		"@typescript-eslint/parser": "^5.13.0",
		"cross-env": "^7.0.3",
		"eslint": "^8.10.0",
		"eslint-import-resolver-node": "^0.3.6",
		"husky": "^7.0.4",
		"jest": "^27.5.1",
		"lint-staged": "^12.3.4",
		"npm-run-all": "^4.1.5",
		"prettier": "^2.5.1",
		"typescript": "^4.6.2"
	},
	"workspaces": [
		"packages/*"
	],
	"resolutions": {
		"react": "^17.0.2",
		"react-dom": "17.0.2",
		"@types/react": "17.0.15",
		"@types/react-dom": "17.0.9"
	}
}
